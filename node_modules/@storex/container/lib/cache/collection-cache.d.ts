import { BaseCache, BaseCacheArgs } from "./base-cache";
import { Collection } from "../collection";
interface CollectionCacheArgs extends BaseCacheArgs {
    id: ((item: any) => string | number) | string;
}
export declare class CollectionCache extends BaseCache {
    protected _getId: (item: any) => string | number;
    data: Collection;
    _itemToId: any;
    constructor(args: CollectionCacheArgs);
    initData(): void;
    /**
     * set the cache
     *
     * @private
     * @returns
     * @memberof Cache
     */
    set(d: any): Promise<void>;
}
export {};
