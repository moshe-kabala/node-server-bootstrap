import { ITimeSpen } from "./models";
export declare class TimeSpen implements ITimeSpen {
    days: number;
    hours: number;
    minutes: number;
    seconds: number;
    constructor(timeSpen?: TimeSpen | number | Object);
    update(timeSpen: any): this;
    initNumber(timeSpen: any): void;
    initTimeSpen(timeS: any): void;
    asView(): string;
    asSeconds(): number;
    clone(): TimeSpen;
}
