import { Where, Sort, FilterFunctions } from "./";
export interface dataTransformArgs {
    data: any[];
    where?: Where[];
    sort?: Sort[];
    page?: number;
    itemsPerPage?: number;
    context: any;
    schema?: any;
    filterFunctions?: FilterFunctions;
}
export declare function filter({ data, where, sort, page, itemsPerPage, schema, context, filterFunctions }: dataTransformArgs): any[];
