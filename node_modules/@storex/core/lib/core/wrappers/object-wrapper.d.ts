import { Dispatcher } from "../dispatcher";
export interface objectWrapper {
    ignores?: string[];
}
export declare function wrapObject<T extends Object>(instance: T, options?: objectWrapper): Dispatcher<T>;
